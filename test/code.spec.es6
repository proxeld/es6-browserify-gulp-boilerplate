import bo, * as opr from '../src/js/modules/code.es6';
import { expect } from 'chai';

describe('Configuration check', function () {
    it('should pass', function () {
        expect(true).to.eql(true);
    })
});